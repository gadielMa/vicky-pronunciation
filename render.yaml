services:
  - type: web
    name: vicky-pronunciation-api
    runtime: go
    rootDir: backend
    buildCommand: go build -o api ./cmd/api
    startCommand: ./api
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
